import { notFound } from "next/navigation";
import { getPublicService } from "@/lib/commercial/data";
import { Breadcrumbs } from "@/components/layout/breadcrumbs";
import { ContactLink } from "@/components/contact-link";
export const dynamic="force-dynamic";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await getPublicService(slug);return s?{title:`${s.title} | Crescimento Vertical`,description:s.shortDescription,alternates:{canonical:`https://crescimentovertical.com/solucoes/${s.slug}`}}:{title:"Solução não encontrada"};}
export default async function Solution({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await getPublicService(slug);if(!s)notFound();const jsonLd={"@context":"https://schema.org","@type":"Service",name:s.title,description:s.shortDescription,url:`https://crescimentovertical.com/solucoes/${s.slug}`,provider:{"@type":"Organization",name:"Crescimento Vertical"}};return <div className="section-pad"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><div className="container-shell"><Breadcrumbs items={[{name:"Início",href:"/"},{name:"Soluções",href:"/solucoes"},{name:s.title,href:`/solucoes/${s.slug}`}]}/><p className="section-kicker">{s.title}</p><h1 className="section-title">{s.positioning||s.shortDescription}</h1><p className="section-copy">{s.shortDescription}</p><div className="grid gap-8 md:grid-cols-2"><Info title="Para quem é" items={[s.targetAudience||"Empresas que buscam estrutura digital."]}/><Info title="Problemas atendidos" items={s.problems}/><Info title="Capacidades" items={s.capabilities}/><Info title="Entregáveis" items={s.deliverables}/><Info title="Processo" items={s.processSteps}/></div><div className="mt-12"><ContactLink className="button-primary">Solicitar diagnóstico ↗</ContactLink></div></div></div>}
function Info({title,items}:{title:string,items:string[]}){return <section><h2 className="text-2xl font-semibold">{title}</h2><ul className="mt-3 space-y-2 text-slate-300">{items.map((x,i)=><li key={`${x}-${i}`}>• {x}</li>)}</ul></section>}
