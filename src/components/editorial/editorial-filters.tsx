import Link from "next/link";
import { CONTENT_TYPE_LABELS, CONTENT_TYPES } from "@/lib/editorial";
export function EditorialFilters({ currentType, currentCategory, currentTag }: { currentType?: string; currentCategory?: string; currentTag?: string }) { const links = [{ label: "Todos", href: "/conteudos" }, ...CONTENT_TYPES.map((type) => ({ label: CONTENT_TYPE_LABELS[type], href: `/conteudos?type=${type}` }))]; return <nav aria-label="Filtros editoriais" className="flex flex-wrap gap-3">{links.map((item) => <Link className={item.href.includes(currentType ?? "__none__") ? "font-semibold underline" : "underline"} href={item.href} key={item.href}>{item.label}</Link>)}{currentCategory ? <span>Categoria: {currentCategory}</span> : null}{currentTag ? <span>Tag: {currentTag}</span> : null}</nav>; }
